<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>求人情報スクレイピングアプリ</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        .job-card {
            transition: transform 0.2s;
        }
        .job-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
        }
        .salary-badge {
            background: linear-gradient(45deg, #28a745, #20c997);
            color: white;
        }
        .remote-badge {
            background: linear-gradient(45deg, #007bff, #6610f2);
            color: white;
        }
        .company-badge {
            background: linear-gradient(45deg, #fd7e14, #e83e8c);
            color: white;
        }
        .source-badge {
            background: linear-gradient(45deg, #6c757d, #495057);
            color: white;
        }
        .hero-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 3rem 0;
        }
        .btn-scrape {
            background: linear-gradient(45deg, #ff6b6b, #ee5a24);
            border: none;
            color: white;
        }
        .btn-scrape:hover {
            background: linear-gradient(45deg, #ee5a24, #ff6b6b);
            color: white;
        }
    </style>
</head>
<body>
    <div class="hero-section">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 mx-auto text-center">
                    <h1 class="display-4 fw-bold mb-4">
                        <i class="fas fa-search"></i> 求人情報スクレイピング
                    </h1>
                    <p class="lead mb-4">
                        Rails / Laravel / Next.js / React の求人情報を自動収集
                    </p>
                    <div class="d-flex justify-content-center gap-3">
                        <a href="/scrape" class="btn btn-scrape btn-lg">
                            <i class="fas fa-download"></i> 求人情報を取得
                        </a>
                        <%# <a href="/search" class="btn btn-outline-light btn-lg">
                            <i class="fas fa-filter"></i> 検索
                        </a> %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container mt-5">
        <% if params[:saved] %>
            <div class="alert alert-success alert-dismissible fade show" role="alert">
                <i class="fas fa-check-circle"></i> 求人情報を保存しました！
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% end %>

        <% if params[:deleted] %>
            <div class="alert alert-info alert-dismissible fade show" role="alert">
                <i class="fas fa-trash"></i> 求人情報を削除しました！
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% end %>

        <% if params[:error] %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-triangle"></i> エラーが発生しました！
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% end %>

        <% if @errors && @errors.any? %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <i class="fas fa-exclamation-triangle"></i> エラーが発生しました：
                <ul class="mb-0">
                    <% @errors.each do |error| %>
                        <li><%= error %></li>
                    <% end %>
                </ul>
                <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>
        <% end %>

        <div class="row">
            <div class="col-12">
                <h2 class="mb-4">
                    <i class="fas fa-bookmark"></i> 保存された求人情報
                    <span class="badge bg-primary ms-2"><%= @saved_jobs.count %></span>
                </h2>
            </div>
        </div>

        <% if @saved_jobs.empty? %>
            <div class="row">
                <div class="col-12">
                    <div class="text-center py-5">
                        <i class="fas fa-inbox fa-3x text-muted mb-3"></i>
                        <h4 class="text-muted">保存された求人情報がありません</h4>
                        <p class="text-muted">「求人情報を取得」ボタンをクリックして、新しい求人情報を取得してください。</p>
                    </div>
                </div>
            </div>
        <% else %>
            <div class="row">
                <% @saved_jobs.each do |job| %>
                    <div class="col-lg-6 col-xl-4 mb-4">
                        <div class="card job-card h-100">
                            <div class="card-header d-flex justify-content-between align-items-center">
                                <span class="badge company-badge">
                                    <i class="fas fa-building"></i> <%= job.company_name %>
                                </span>
                                <span class="badge source-badge">
                                    <%= job.source_site %>
                                </span>
                            </div>
                            <div class="card-body">
                                <h5 class="card-title">
                                    <a href="<%= job.url %>" target="_blank" class="text-decoration-none">
                                        <%= job.title %>
                                    </a>
                                </h5>
                                <p class="card-text text-muted">
                                    <%= job.description&.length && job.description.length > 100 ? "#{job.description[0..96]}..." : job.description %>
                                </p>
                                <div class="mb-3">
                                    <span class="badge salary-badge me-2">
                                        <i class="fas fa-yen-sign"></i> <%= job.salary_range %>
                                    </span>
                                    <span class="badge remote-badge">
                                        <i class="fas fa-home"></i> <%= job.remote_work_text %>
                                    </span>
                                </div>
                                <% if job.location %>
                                    <p class="card-text">
                                        <small class="text-muted">
                                            <i class="fas fa-map-marker-alt"></i> <%= job.location %>
                                        </small>
                                    </p>
                                <% end %>
                            </div>
                            <div class="card-footer bg-transparent">
                                <div class="d-flex justify-content-between">
                                    <a href="<%= job.url %>" target="_blank" class="btn btn-outline-primary btn-sm">
                                        <i class="fas fa-external-link-alt"></i> 詳細を見る
                                    </a>
                                    <form action="/jobs/<%= job.id %>" method="post" class="d-inline">
                                        <input type="hidden" name="_method" value="delete">
                                        <button type="submit" class="btn btn-outline-danger btn-sm" 
                                                onclick="return confirm('この求人情報を削除しますか？')">
                                            <i class="fas fa-trash"></i> 削除
                                        </button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                <% end %>
            </div>
        <% end %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
